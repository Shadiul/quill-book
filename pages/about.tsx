import Layout from "../components/Layout";

type Props = {};

const about = (props: Props) => {
  return (
    <Layout>
      <h1>Welcome to QuillBook</h1>
    </Layout>
  );
};

export default about;
